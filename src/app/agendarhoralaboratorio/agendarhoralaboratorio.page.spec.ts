import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgendarhoralaboratorioPage } from './agendarhoralaboratorio.page';

describe('AgendarhoralaboratorioPage', () => {
  let component: AgendarhoralaboratorioPage;
  let fixture: ComponentFixture<AgendarhoralaboratorioPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgendarhoralaboratorioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
